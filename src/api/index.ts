import {
	ItrainsApi,
	trainsUrlEndpoint,
	getTrains,
	addTrain,
	updateTrain,
	deleteTrain,
} from "./trainsApi";

export { addTrain, deleteTrain, getTrains, trainsUrlEndpoint, updateTrain };
export type { ItrainsApi };
